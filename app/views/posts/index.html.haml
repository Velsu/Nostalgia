- @posts.each do |post|
	%h1= link_to image_tag(post.image.url(:small)), post
	%h1= link_to post.title, post
	%p= pluralize(post.comments.size, "Comment")
	%p= pluralize(post.get_upvotes.size, "Like")

= link_to "Add Nostalgia", new_post_path